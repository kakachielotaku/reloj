let segundos = 0
segundos += 1
basic.forever(function () {
    let minutos = 0
    if (segundos >= minutos) {
        basic.showNumber(segundos)
        basic.pause(100)
        segundos += 59
    } else {
    	
    }
})
